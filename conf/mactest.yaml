defaults:
  - config

device: "cpu"

#player_0:
  #tokenizer: 'distilbert-base-uncased'
  #model: 'distilbert-base-uncased'

#player_1:
  #tokenizer: 'distilbert-base-uncased'
  #model: 'distilbert-base-uncased'
